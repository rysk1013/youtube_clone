import { makeStyles } from '@material-ui/core';

export default makeStyles({
  label: {
    display: 'block',
    paddingBottom: 40,
  },
  button: {
    display: 'flex',
    justifyContent: 'center',
  },
});
